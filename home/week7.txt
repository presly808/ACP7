1.  XML -> DOM, SAX parsers
    DTD, XSD -> describe xml structure
   
   build tool Maven
   folder structure, pom.xml, M2_HOME system variable
   lifecycles->plugins->goals
   POM -> dependecies, properties

   JPA -> Hibernate -> EntityManagerFactory -> EntityManager
   JPA Entity Lifecycle->EntitManager Context
   JPQL
   Transaction

   EE project structure:  View->Service->DAO->DB
   
2. Implement xml serializer
   Object -> XML
   XML -> Object
   
   use Dom parser

3. Keep coding own project
   if you dont have an idea.

   Do last homework (notebook show week5)
   but with JPA

4. Wish you good week

 
